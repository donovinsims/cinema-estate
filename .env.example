# Sequenzy form endpoint the waitlist form posts to. Required at build time
# (it's a NEXT_PUBLIC_ var, inlined into the client bundle) for the Vercel
# deployment — set this in the Vercel project's Environment Variables before
# deploying. Without it, the form shows a graceful "being connected" fallback.
NEXT_PUBLIC_SEQUENZY_FORM_ENDPOINT=
